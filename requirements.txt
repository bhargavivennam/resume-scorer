# Core ML
scikit-learn>=1.4
lightgbm>=4.3
numpy>=1.26
scipy>=1.11
joblib>=1.3

# Document parsing (PDF / DOCX resumes, job-posting URLs)
pypdf>=4.2
python-docx>=1.1
requests>=2.31
beautifulsoup4>=4.12

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9

# Tests
pytest>=8.0
httpx>=0.27

# ---------------------------------------------------------------------------
# Optional. Everything above is enough to run the whole project; each extra
# below turns on one feature and degrades to a no-op when absent.
# ---------------------------------------------------------------------------

# Semantic matching (semantic.py). OFF unless RESUME_SCORER_SEMANTIC=1.
# Measured worth ~1 point on a real resume/posting pair, and torch conflicts
# with LightGBM's OpenMP runtime on macOS - run its tests separately:
#   pytest tests --ignore=tests/test_semantic.py
#   pytest tests/test_semantic.py
# sentence-transformers>=3.0

# LLM matching (llm_matcher.py, compare_matchers.py). Needs ANTHROPIC_API_KEY.
# Costs real money per call.
# anthropic>=1.0
