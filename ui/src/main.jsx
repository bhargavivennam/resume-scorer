import React from 'react'
import ReactDOM from 'react-dom/client'
import ResumeScorer from './ResumeScorer.jsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ResumeScorer />
  </React.StrictMode>
)
